<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
</head>

<body>
    <form action="/register" method="post">
        <div>
            <div>
                <label for="firstName">First name</label>
                <input text name="firstName" id="firsName" required>
            </div>
            <div>
                <label for="lastName">Last name</label>
                <input text name="lastName" id=lastName required>
            </div>
            <div>
                <label for="userName">User name</label>
                <input text name="useerName" id="userName" required>
            </div>
            <div>
                <label for="emailOrPhone">Register</label>
                <input id="emailOrPhone" type=text class="form-control {{@erorr->('phone') @error->has('email')  @error->has('name') ? 'is-invalid' : '' }}" name="email" value="{{ old('phone') ?: old('email') ?: old('name') }}" required autofocus}}">
            </div>
            <div>
                <label for="password">Password</label>
                <input password name="password" id="password">
            </div>
        </div>
        <div>
            <input type="submit" value="Register">
            
        </div>    
    </form>
<div>
    <a href="/views/login.ejs">to login</a>
</div>
</body>

</html>